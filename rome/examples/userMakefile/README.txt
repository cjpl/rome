This example shows how to use own class by using Makefile.usr.
However, it is recommended to add own classes in your definition XML files rather than Makefile.usr.

Please have a look at the example 'tree_write_read2'.
